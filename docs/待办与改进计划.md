# Awen 待办与改进计划(2026-09-21 整理)

> 来源:2026-09-20/21 多轮实测反馈与 Word/WPS 对比、设计文档对照。
> 状态标记:⬜ 未开始 / ◐ 部分 / ✅ 已完成。规模:S=小时级 M=半天~1天 L=多天。
> 原则:每批改动后浏览器+桌面(CDP)双端实测,再进下一批。

## A. 桌面壳体验(草创感硬伤,P0)

| # | 项 | 内容 | 涉及 | 规模 |
|---|---|---|---|---|
| A1 | 闪黑窗修复 ⬜ | aine.exe 是控制台程序,spawn 加 `CREATE_NO_WINDOW`(0x08000000);启动时 refreshNativeDiags+adoptNativeBlocks 两次 IPC 合并为一次 spawn | main.rs | S |
| A2 | 单实例 ⬜ | tauri-plugin-single-instance:二次启动把参数转发给已有窗口并聚焦,不再开第二个编辑器 | Cargo/main.rs | S |
| A3 | 命令行打开文件 ⬜ | main.rs 读 argv 文件路径 → 读盘+解容器 → emit 给前端 → 复用现有"复用空白标签/新开标签"逻辑 | main.rs/bridge.js/template.html | M |
| A4 | .awen 文件关联 ⬜ | tauri.conf bundle fileAssociations 注册 .awen;双击文件=系统级打开 | tauri.conf.json | S |
| A5 | 图标接入 ⬜ | image/awen.ico(6尺寸)→ icons/icon.ico;awen.png 缩出生成 32x32.png/128x128.png;重打包后 exe/任务栏/安装器生效 | icons/ | S |

验收(A 组):双击 3 个 .awen 文件 → 一个窗口 3 个标签;启动/编辑全程无黑窗;任务栏/窗口/安装器为新图标。

## B. 多文件(已完成基线上的收尾)

| # | 项 | 状态 | 说明 |
|---|---|---|---|
| B1 | 启动空白标签/开文件复用/新建对称 | ✅ 71cfb9e | 双端实测过 |
| B2 | 打开保存/关闭激活/撤销隔离/红点 | ✅ 6d83e9c | 双端实测过 |
| B3 | 双击文件进标签(依赖 A2/A3/A4) | ⬜ | A 组完成后天然闭环 |

## C. 功能区 UI 重构(P1,观感主诉)

| # | 项 | 内容 | 规模 |
|---|---|---|---|
| C1 | 布局归位 ⬜ | 撤销/重做移标题栏左侧快速访问区;视图系(显示/语法/分屏/专注/⇄)从 ribbon 标签行挪走(独立"视图"选项卡或标题栏右端小图标组);引用/分隔线按钮移入"插入" | M |
| C2 | 折叠交互 ⬜ | 删标签行正中的 ▲;改 ribbon 右下角常驻小 chevron + 双击标签名折叠/展开(Word 同款) | S |
| C3 | 开始页补件 ⬜ | 剪贴板组:粘贴/选择性粘贴/格式刷(execCommand 可做);字体组:增大/减小字号;段落组:增减缩进/行距按钮/多级列表;编辑组:替换独立入口/全选 | M |
| C4 | 样式独立成组 ⬜ | 样式下拉从字体组拆出;option 排版预览;后续可升级样式库 | S~M |
| C5 | 插入页补全 ⬜ | 数学(@[m])、书签(@[label])、日期、符号入口——引擎语法已有,只缺 UI | M |
| C6 | 文件页 Backstage 化 ⬜ | 另存为、最近文件(localStorage 起步)、文档信息 | M |
| C7 | 视觉细节 ⬜ | 颜色按钮一体 split(点色块默认色/点三角开面板);图标风格统一(字母 B/I/U 换 SVG);组密度均衡 | M |

## D. 功能缺口(P1.5~P2,按 Word/WPS 对比)

| # | 项 | 优先级 | 说明 |
|---|---|---|---|
| D1 | 目录渲染 | ✅ 高 | @[toc depth] 按标题生成/点击跳转/序列化回写;空文档提示 |
| D2 | 最近文件 | ✅ 高 | 文件页展示;桌面版带路径直开;浏览器版置灰提示 |
| D3 | 打印修复 | ✅ 中 | @media print 重置 zoom,保证 100% 出纸 |
| D4 | 自动保存/崩溃恢复 | ✅ 中 | 2s 快照+启动恢复提示(继续/放弃草稿)+文件页入口 |
| D5 | docx 导入 | ✅ 中 | mammoth 本地分发(离线可用)+HTML→Awen 转换器(标题/粗斜/列表/引用/表格/链接) |
| D6 | 图片属性面板 | ✅ 中 | 点击图片→宽度/对齐面板→写回源码行(双方言参数) |
| D7 | Label 重命名 | ✅ 中 | 标签面板内联改名,同步替换全部 @[label]/@[ref] |
| D8 | 表格跨页表头/排序/公式 | ⬜ 低 | |
| D9 | 图片环绕/裁剪、形状、艺术字 | ⬜ 低 | 渲染模型限制,需规范先行 |
| D10 | 手册全面重做(**最后做**) | 高 | 现状:只有 18~42 章(重复副本已删,ch22 笔误已修)。**必须排在所有功能/UX 批次完成后**:①补写第 1~17 章新手内容(安装启动/界面认识/第一次编辑/保存打开/基础排版/视图模式)②把届时已完成的全部新功能与 UI 变更写进去(功能区重构后布局/视图选项卡/单实例/文件关联/图标/文档级设置/目录渲染等)③**清除全部过时描述**,不允许新旧并存④全书 42 章重新编号校对+目录⑤改用单一生成脚本统一产出,不再手工追加。完成后开 GitHub Pages 上线首页入口 | L |

## E. 规范/引擎层缺口(P2,与设计文档对照)

| # | 项 | 说明 | 规模 |
|---|---|---|---|
| E1 | 文档级设置生效 | @[margin 20mm]/@[font]/@[size]/@[line-spacing]/@[first-line] 源码设置前端只显示灰字不生效;应驱动纸面(与工具栏全局设置合并优先级:文档级>全局) | M |
| E2 | 撤销接引擎 undo.aine | E1-5 设计路径;现前端自研 hist(语义步已补)。决策:接入 or 承认偏离并回写设计文档 | M~L |
| E3 | 选区映射接入 | inline_span_map 闲置;接入后可做"选区显示↔源码高亮"与 P-09 场景 | L |
| E4 | 引擎/前端分页一致性 | 前端像素实测=真实字体(达 E3-1 目的);引擎 page_flow 均匀模型→两端页码可能不一致;导出/打印路径以哪端为准需决策 | M |
| E5 | 段间距 | 规范未定义段距语法 | 依赖规范 |
| E6 | 增量布局接入 | page_flow_incremental 闲置;前端每键全量重排(当前文档规模够用) | M |
| E7 | .awen 容器资源 chunk | E4-3:图片现在是 data URI 塞源码行;容器带资源块+格式迁移 | L |
| E8 | 完整 manifest 保存 | 前端 doSave 固定头,无 nodes/next_id;两端兼容但清单不全(改走 save_awen 等价输出) | S |

## F. 规范待定(先定语法再实现,不自造)

- 页眉页脚 / 分栏 / 水印 / 页码域
- 嵌套列表(引擎解析器不支持层级)
- 字符间距 / 段间距
- 稿纸 / 拼音指南等中文特化

## G. 工程收尾

- G1 设计文档回填:决定点 1/2(GUI=Web+Tauri、双视图)已定;P3-3 状态;测试数 445;core_blocks/tauri_cli 桥接层
- G2 性能建档:§45 预算基准(解析/渲染/IPC 耗时实测)
- G3 手册更新:新功能(专注/多文件模型/缩放自定义/启动空白)与截图
- G4 json_escape \b\f 补齐(低优先,文本文档罕见)

## 建议实施顺序

1. **第一批(壳体验)**:A1 闪窗 → A5 图标 → A2/A3/A4 单实例+关联+argv(多文件闭环)
2. **第二批(UI 重构)**:C1 布局归位 → C2 折叠 → C3/C4 开始页 → C7 视觉
3. **第三批(功能)**:C5 插入页 → C6/D2 文件页+最近文件 → D1 目录 → D3 打印
4. **第四批(规范/引擎)**:E1 文档级设置 → E8 manifest → E2/E3 决策 → F 规范项逐个冻结
5. **第五批(手册,最后)**:D10 手册全面重做——吸收前四批全部变更,清旧内容,开 Pages 上线
